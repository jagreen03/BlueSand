name: BlueSand scan
on:
#  workflow_dispatch:
#  push:
#    paths: ["config/**","scripts/**"]
#jobs:
#  scan:
#    runs-on: windows-latest
#    steps:
#      - uses: actions/checkout@v4
#      - name: Run BlueSand scan
#        shell: pwsh
#        run: scripts/BlueSand-Scan.ps1
#      - name: Upload artifacts
#        uses: actions/upload-artifact@v4
#        with:
#          name: bluesand-wordmap
#          path: docs/WORDMAP_*
